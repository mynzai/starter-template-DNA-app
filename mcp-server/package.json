{
  "name": "dna-cli-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for DNA Template CLI integration with Claude Desktop",
  "main": "dist/dna-cli-mcp-server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/dna-cli-mcp-server.js",
    "install-global": "npm run build && npm link",
    "setup": "npm install && npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "dna-cli",
    "claude",
    "ai",
    "templates"
  ],
  "author": "DNA Templates Team",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}